{
  "name": "lindia-fullstack",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev"
  },
  "description": "LegalIndia.ai full-stack application with separate frontend and backend"
}
